/*
 * LabelledCallList Messages
 *
 * This contains all the text for the LabelledCallList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LabelledCallList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LabelledCallList component!',
  },
});
