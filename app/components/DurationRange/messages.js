/*
 * DurationRange Messages
 *
 * This contains all the text for the DurationRange component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DurationRange';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DurationRange component!',
  },
});
