/*
 * AgentList Messages
 *
 * This contains all the text for the AgentList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AgentList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgentList component!',
  },
});
