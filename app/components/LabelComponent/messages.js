/*
 * LabelComponent Messages
 *
 * This contains all the text for the LabelComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LabelComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LabelComponent component!',
  },
});
